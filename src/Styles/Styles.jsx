const defaultStyles = {
    minWidth: 260,
    mr: 2,
    mb: 2
};

const executeSelectFormControlStyles = {
    width: 170,
    mt: "5px",
    ml: '20px'
};

const noRowsOverlayStackStyles = {
    height: '100%',
    alignItems: 'center',
    justifyContent: 'center',
    fontSize: '20px',
    fontWeight: 'bolder',
    color: 'black'
};

const cellStyles = {
    headerAlign: 'center',
    align: 'center',
    sortable: false,
    filterable: false,
};

const defaultContainerStyles = {
    py: 5,
    minHeight: "100vh",
    textAlign: "center"
};

const defaultBoxStyles = {
    my: 5,
    mx: "auto",
    width: "25%",
    bg: "white",
    borderRadius: 3,
    p: 4
};

const defaultFormStyles = {
    mb: 2
};

const overlayLoading = {
    display: 'flex',
    background: 'rgba(255, 255, 255, 0.95)',
    position: 'fixed',
    bottom: 0,
    left: 0,
    right: 0,
    top: 0,
    alignItems: 'center',
    justifyContent: 'center',
    zIndex: 1,
    width: '100%',
    height: '100%'
};

const containerStyles = {
    py: 5,
    background: "#ffffff",
    minHeight: "100vh",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
};

const boxStyles = {
    maxWidth: 400,
    width: "100%",
    textAlign: "center",
    bg: "white",
    borderRadius: 3,
    p: 4,
};

const textFieldStyles = {
    mb: 2,
};

const buttonStyles = {
    mt: 2,
    bgcolor: "#ffd333",
};

const TextFieldStyle = {
    pb: 3.2,
    fontWeight: 'bolder',
    mt: 0.5,
    ml: 1,
    width: 260
}

const gridHeader = {
    pb: 1,
    fontWeight: 'bolder',
    mt: 1
}

const headerIcons = {
    mr: 2,
    mt: 2.5,
    cursor: 'pointer'
}

const datagridBox = {
    height: 430,
    width: '100%',
    mb: 2
};

export {
    defaultStyles,
    executeSelectFormControlStyles,
    noRowsOverlayStackStyles,
    cellStyles,
    defaultContainerStyles,
    defaultBoxStyles,
    defaultFormStyles,
    overlayLoading,
    containerStyles,
    boxStyles,
    textFieldStyles,
    buttonStyles,
    gridHeader,
    headerIcons,
    datagridBox,
    TextFieldStyle
};